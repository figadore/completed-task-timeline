import { Timeline } from "@/components/Timeline";

const Index = () => {
  return (
    <div className="min-h-screen bg-[image:var(--timeline-bg)]">
      <Timeline />
    </div>
  );
};

export default Index;
